# Local development — override the auto-derived signaling URL.
# React dev server runs on :3000; backend runs on a different port.
#   Spring Boot backend: ws://localhost:8080/signal
#   Node.js server:      ws://localhost:3000/signal
REACT_APP_SIGNAL_URL=ws://localhost:8080/signal
